services:
  frontend:
    container_name: portfolio_frontend
    build:
      context: ./Frontend
      dockerfile: Dockerfile
    ports:
      - "5174:80"  # host 5174 → contenedor 80
    restart: unless-stopped
